Installation
============
To install from repository to the system use:
`make -f Makefile.cvs`
`make`
`sudo make install`

Creating Package
================
`make package`
or
`make package-local`

Run
===
`yast2 sshd`
